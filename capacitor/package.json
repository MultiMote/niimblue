{
  "name": "niimblues",
  "version": "1.0.0",
  "description": "NiimBlue standalone app",
  "type": "module",
  "scripts": {
    "build-www": "cd .. && vite build --outDir ./capacitor/www",
    "build-android": "cap sync && cap build android",
    "run-android": "cap sync && cap run android --device",
    "build-ios": "cap sync && cap open ios",
    "run-ios": "cap sync && cap run ios"
  },
  "dependencies": {
    "@byteowls/capacitor-filesharer": "^6.0.0",
    "@capacitor-community/bluetooth-le": "^6.0.2",
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  },
  "author": "MultiMote",
  "license": "MIT"
}
